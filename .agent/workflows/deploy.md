---
description: How to build and serve the production application
---

1. Build the production application
// turbo
`npm run build`

2. After building, you can verify the production build locally
`npm run preview`

3. To host it permanently, copy the contents of the `dist` folder to your web server.
